class ProductoDTO {
    constructor({ title, price, thumbnail }) {
        this.title = title,
            this.price = price,
            this.thumbnail = thumbnail
    }
}

export default ProductoDTO;